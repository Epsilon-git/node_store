$(document).ready(function () {
  $(".owl-carousel").owlCarousel({
    // loop: true,
    // center: true,
    margin: 10,
    items: 1,
    nav: false,
    dots: true,
    autoWidth: true,
  });
});

$(document).ready(function () {
  $(".owl-carousel_card").owlCarousel({
    items: 1,
    nav: false,
    dots: false,
    autoWidth: true,
  });
});
